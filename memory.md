本次会话改动摘要（2026-06-06）：

- 扫描去重：同时匹配 file_path 和 file_name，防止文件移动后重复导入
- 前端表格/详情弹窗显示 hostFilePath（Windows宿主机路径）
- scan_bp 补 url_prefix='/api/scan'，修复后端启动崩溃
- 定时提醒：settings 保存 reminder 时调用 reset_send_state() 重置发送状态
- 文件导出：后端存 /data/download/，前端弹窗展示路径，用户点确定关闭
- 附件上传已从合同/专利/车险新增编辑页移除
- 表头排序：合同/专利/车险支持按到期时间、名称、重要程度点击排序，后端白名单防注入，前端 ⇅/▲/▼ 图标
- 信息完整性：is_complete 字段，扫描导入=0（不完整），手动录入=1（完整）；必填字段缺失标记 ⚠️ 不完整
